from setuptools import setup

setup(name='findzebra_flow',
      version='0.1',
      description='Flow framework for text classification',
      author='Dan Svenstrup',
      license='MIT',
      packages=['flow_framework'],
      zip_safe=False)